import { CardColumnDirective } from './card-column.directive';

describe('CardColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new CardColumnDirective();
    expect(directive).toBeTruthy();
  });
});
